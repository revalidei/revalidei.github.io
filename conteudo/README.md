# Banco de questões

Cada arquivo `revalida_YYYY_N.json` alimenta o simulado da edição correspondente em `pages/PROVAS/`.

**Estado atual:** as edições sem conteúdo próprio usam temporariamente o mesmo conjunto de `revalida_2022_1.json` (100 questões). Substitua cada arquivo pelo JSON oficial da edição quando disponível.
